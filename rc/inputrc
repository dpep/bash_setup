# customize autocompletion, readline, and key bindings
# to make your general CLI experience more efficient.
# `bind -v` to see all settings
# `bind -f ~/.inputrc` to reload


# do not expand ~ to $HOME during autocompletion,
# eg `ls ~/<tab tab>`
set expand-tilde off

# make autocompletion case insensitive
# eg. `cat myconfi<tab tab>` -> `cat myConfig.yml`
set completion-ignore-case on

# underscores and hyphens are interchangeable
# during autocompletion
set completion-map-case on
